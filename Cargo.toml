[package]
name = "gws-core"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0 OR MIT"
rust-version = "1.95"

[lib]
path = "src/lib.rs"

[dependencies]
git2 = { version = "0.21", features = ["https", "ssh"] }
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
