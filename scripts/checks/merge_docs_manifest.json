{
  "manifest_version": 1,
  "description": "R0 canonical merge assertions. Every row pins a statement a user-facing or historical document must (or must not) make about the merge surface. Rows naming M5a-A4 milestones survive only where they pin a HISTORICAL document that accurately records what was true at its own date; a row that pins user-facing prose states what the tool does today.",
  "global_forbidden": [
    {
      "id": "m5a_must_not_release_no_ff",
      "match": "regex",
      "value": "(?i)\\bM5a\\b\\s+(?:ships|releases|adds|activates|includes|advertises|writes)\\s+(?:`?--no-ff`?|no-ff)\\b",
      "comment": "M5d 2026-09-05 review: KEPT, not retired. The test applied was whether the row can fire on a sentence that is now TRUE -- that is what made `releases_must_not_claim_no_ff_yet` stale (see `retired` below). This row cannot: `--no-ff` was never released at M5a and never had a writable path under record v0, so no correct document, historical or user-facing, can say so. It is narrow, not stale, and it is also part of what keeps milestone vocabulary out of shipped prose. This row additionally backs the gate's own negative control (`test_check_merge_docs.py::test_deliberate_v0_no_ff_claim_fails_the_real_gate`), which is the only proof the checker catches anything at all."
    },
    {
      "id": "no_ff_must_not_be_available_at_m5a",
      "match": "regex",
      "value": "(?i)(?:`?--no-ff`?|no-ff)\\s+is\\s+(?:now\\s+)?(?:available|supported|released|writable)\\s+(?:in|at|under)\\s+M5a\\b",
      "comment": "M5d 2026-09-05 review: KEPT, not retired. The test applied was whether the row can fire on a sentence that is now TRUE -- that is what made `releases_must_not_claim_no_ff_yet` stale (see `retired` below). This row cannot: `--no-ff` was never released at M5a and never had a writable path under record v0, so no correct document, historical or user-facing, can say so. It is narrow, not stale, and it is also part of what keeps milestone vocabulary out of shipped prose."
    },
    {
      "id": "no_ff_must_not_be_available_in_v0",
      "match": "regex",
      "value": "(?i)(?:`?--no-ff`?|no-ff)\\s+is\\s+(?:now\\s+)?(?:available|supported|released|writable)\\s+(?:in|at|under)\\s+(?:record\\s+)?v0\\b",
      "comment": "M5d 2026-09-05 review: KEPT, not retired. The test applied was whether the row can fire on a sentence that is now TRUE -- that is what made `releases_must_not_claim_no_ff_yet` stale (see `retired` below). This row cannot: `--no-ff` was never released at M5a and never had a writable path under record v0, so no correct document, historical or user-facing, can say so. It is narrow, not stale, and it is also part of what keeps milestone vocabulary out of shipped prose."
    },
    {
      "id": "m5b_must_not_release_v0",
      "match": "regex",
      "value": "(?i)\\bM5b\\b\\s+(?:ships|releases|activates|writes)\\s+(?:`?--no-ff`?|no-ff)[^.\\n]{0,80}\\bv0\\b",
      "comment": "M5d 2026-09-05 review: KEPT, not retired. The test applied was whether the row can fire on a sentence that is now TRUE -- that is what made `releases_must_not_claim_no_ff_yet` stale (see `retired` below). This row cannot: `--no-ff` was never released at M5a and never had a writable path under record v0, so no correct document, historical or user-facing, can say so. It is narrow, not stale, and it is also part of what keeps milestone vocabulary out of shipped prose."
    },
    {
      "id": "retired_compound_stash_phase",
      "value": "normalize_root"
    },
    {
      "id": "retired_compound_restore_phase",
      "value": "restore_root"
    },
    {
      "id": "retired_root_prefix_field",
      "value": "root_publication_prefix"
    }
  ],
  "sources": [
    {
      "id": "proposal",
      "path": "dev-docs/GwzM5-8Refactor.md",
      "required": [
        {
          "id": "proposal_m5a_defers_no_ff",
          "value": "M5a custom-message slice approved; `--no-ff` deferred to v1/A1"
        },
        {
          "id": "proposal_m5b_a1_proves_v1_no_ff",
          "value": "M5b/A1 proves v1 no-ff."
        },
        {
          "id": "proposal_v0_scope",
          "value": "| v0 | Released M4 integration plus M5 custom messages using existing commit-message recovery authority | M4/M5a custom-message slice |"
        },
        {
          "id": "proposal_v1_scope",
          "value": "| v1 | v0 semantics plus executable `--no-ff`, persisted `AcceptedWorkspace`, born/unborn acceptance, publication/no-publication consumption, typed reverse/preservation journals, and recovery origin | A1 after R4b |"
        },
        {
          "id": "proposal_v2_scope",
          "value": "| v2 | v1 plus M6 create/switch/integrate/restore/delete branch lifecycle and ownership-aware rollback | M6 activation |"
        },
        {
          "id": "proposal_v3_scope",
          "value": "| v3 | v2 plus M7 snapshot-source provenance, resolution, and reporting | M7 activation |"
        },
        {
          "id": "proposal_v4_scope",
          "value": "| v4 | v3 plus M8 required/optional participation, skipped outcomes, partial composition, and its recovery/reporting semantics | M8 activation |"
        },
        {
          "id": "proposal_m5a_rejects_before_record",
          "value": "M5a rejects `--no-ff` before record creation;"
        }
      ],
      "forbidden": []
    },
    {
      "id": "r4b_transition_design",
      "path": "dev-docs/GwzM5-8R4bTransitionDesign.md",
      "required": [
        {
          "id": "r4b_no_mutable_record_authority",
          "value": "no lifecycle module receives mutable access to a v1 record;"
        },
        {
          "id": "r4b_only_kernel_prepares_rewrite",
          "value": "only the transition kernel can construct a persistable v1 rewrite;"
        },
        {
          "id": "r4b_mutation_requires_owner",
          "value": "every physical Git or filesystem mutation has a persisted typed owner before it begins;"
        },
        {
          "id": "r4b_v0_v1_isolation",
          "value": "v0 remains on its released lifecycle and store; v1 does not execute through `v0_common_view`;"
        },
        {
          "id": "r4b_no_generic_writer",
          "value": "There is no `write_v1(record)`, `persist_v1(record)`, `update_v1(fn)`, public `validate_transition(old, new)`, or public constructor for a prepared rewrite."
        },
        {
          "id": "r4b_mutation_lease",
          "value": "The lease is retained from before the first open-record read through every observation,"
        },
        {
          "id": "r4b_observation_resolver",
          "value": "fn resolve_observation("
        },
        {
          "id": "r4b_proof_binding",
          "value": "struct ProofBinding {"
        },
        {
          "id": "r4b_single_forward_owner",
          "value": "at most one forward `pending_action`"
        },
        {
          "id": "r4b_preservation_exhaustion",
          "value": "VerifiedPreservationExhausted"
        },
        {
          "id": "r4b_preservation_cursor_prefix",
          "value": "VerifiedPreservationCursorPrefix"
        },
        {
          "id": "r4b_exact_dependency_dag",
          "value": "R4b-TI + R4b-TR + R4b-S + R4b-A -> R4b-X"
        },
        {
          "id": "r4b_a1_only_activation",
          "value": "A1 remains a separate, small, reviewed runtime-dispatch change after R4b and M5b meet their gates."
        }
      ],
      "forbidden": []
    },
    {
      "id": "i2_action_journal_contract",
      "path": "dev-docs/GwzM5-8I2ActionJournalContract.md",
      "required": [
        {
          "id": "i2_journal_amended_wire_authority",
          "value": "This document is wire-authoritative only as amended by those two accepted interfaces."
        },
        {
          "id": "i2_journal_handoff_field",
          "value": "root_publication_handoff: Option<PreservationPublicationCandidateV1>"
        },
        {
          "id": "i2_journal_flat_stash_graph",
          "value": "normalize_parent -> normalize_marker -> normalize_lock -> normalize_index"
        },
        {
          "id": "i2_journal_flat_reset_graph",
          "value": "prepare_parent -> prepare_marker -> prepare_lock -> prepare_index"
        }
      ],
      "forbidden": [
        {
          "id": "i2_journal_no_retired_compound_stash_phase",
          "value": "NormalizeRoot"
        },
        {
          "id": "i2_journal_no_retired_compound_reset_phase",
          "value": "RestoreRoot"
        },
        {
          "id": "i2_journal_no_retired_prefix_field",
          "value": "root_publication_prefix"
        }
      ]
    },
    {
      "id": "i2_record_contract",
      "path": "dev-docs/GwzM5-8I2RecordContract.md",
      "required": [
        {
          "id": "i2_record_amended_wire_authority",
          "value": "This document is wire-authoritative only as amended by those accepted interfaces."
        },
        {
          "id": "i2_record_five_fields",
          "value": "It adds five top-level fields:"
        },
        {
          "id": "i2_record_handoff_field",
          "value": "preservation_publication_handoff: Option<PreservationPublicationHandoffV1>"
        },
        {
          "id": "i2_record_five_collisions",
          "value": "five top-level v1 collisions"
        }
      ],
      "forbidden": [
        {
          "id": "i2_record_no_four_field_claim",
          "value": "adds four top-level fields"
        },
        {
          "id": "i2_record_no_retired_prefix_field",
          "value": "root_publication_prefix"
        }
      ]
    },
    {
      "id": "i2_compatibility_contract",
      "path": "dev-docs/GwzM5-8I2CompatibilityContract.md",
      "required": [
        {
          "id": "i2_compat_a1_production_archive_split",
          "value": "the archived-v0 decoder, neutral archive projection, and canonical record-location acquisition are normal-build code consumed by ordinary status and GC"
        },
        {
          "id": "i2_compat_amended_boundary_authority",
          "value": "this document is boundary-authoritative only as amended by that accepted interface."
        }
      ],
      "forbidden": []
    },
    {
      "id": "merge_plan",
      "path": "gwz-core/dev-docs/GwzMergePlan.md",
      "required": [
        {
          "id": "plan_m5a_custom_only",
          "value": "The current release slice is M5a custom messages only. `--no-ff` is not writable or releasable under record v0"
        },
        {
          "id": "plan_m5b_disabled_v1",
          "value": "it is implemented as M5b behind the disabled v1 writer and activates only with A1"
        },
        {
          "id": "plan_m5a_release_boundary",
          "value": "M5a ships only `-m`/custom-message behavior under v0 after M4 and rejects `--no-ff` before record creation."
        },
        {
          "id": "plan_a1_atomic_activation",
          "value": "A1 atomically activates the v1 writer, eligible v0 migration, and deterministic `--no-ff` start surface."
        },
        {
          "id": "plan_i6_v2",
          "value": "I6/M6/A2 owns `--into` and cannot begin implementation until coordinated switch-plus-merge rollback has an accepted v2 design."
        },
        {
          "id": "plan_i7_v3",
          "value": "I7/M7/A3 owns exact per-member snapshot sources and the v3 wire/archive/downgrade contract."
        },
        {
          "id": "plan_i8_v4",
          "value": "I8/M8/A4 owns merge partial/skip semantics"
        },
        {
          "id": "plan_gate_runs_every_wave",
          "value": "R0 adds and every M5a–A4 gate runs the merge document-consistency check."
        }
      ],
      "forbidden": []
    },
    {
      "id": "merge_design",
      "path": "gwz-core/dev-docs/GwzMergeDesign.md",
      "required": [
        {
          "id": "design_m5a_custom_only",
          "value": "M5a adds v0-safe custom messages. Deterministic `--no-ff` is M5b under record v1 and activates only with A1"
        },
        {
          "id": "design_no_v0_no_ff_path",
          "value": "it has no writable or releasable v0 path."
        },
        {
          "id": "design_i6_i7_i8_sequence",
          "value": "I6/M6/A2 separately owns target-branch switching, I7/M7/A3 adds exact snapshot sources, and I8/M8/A4 owns any explicit merge partial/skip policy."
        },
        {
          "id": "design_request_table_versions",
          "value": "`normal` now, `ff_only` from M4, `no_ff` from M5b/A1 under v1"
        },
        {
          "id": "design_m5a_rejects_before_record",
          "value": "Reject `--no-ff` before record creation and never write `mode: no_ff` under v0."
        },
        {
          "id": "design_m5b_no_v0_release",
          "value": "Give M5b no independently releasable v0 path."
        },
        {
          "id": "design_a1_atomic_activation",
          "value": "Activate the v1 writer, eligible v0 migration, and no-ff start surface together only at A1."
        },
        {
          "id": "design_later_wave_phases",
          "value": "### Phase I6/M6/A2: explicit target branch"
        }
      ],
      "forbidden": []
    },
    {
      "id": "operation_model",
      "path": "gwz-core/docs/OperationModel.md",
      "required": [
        {
          "id": "operation_model_merge_runs_everywhere",
          "value": "A merge runs on every filesystem. Crash recovery is a capability, not a gate."
        },
        {
          "id": "operation_model_crash_recovery_warning_sentence",
          "match": "regex",
          "comment": "M5d step (3): the sentence is unchanged and still pinned whole. On a volume without persistent file handles the SAME diagnostic gains an appended clause (charter §3, one Diagnostic and not two), so the pattern now states explicitly that the clause may follow -- matching is `re.search`, so this admitted both forms before and after; the alternation is here so a reader of the pin knows which two forms are the product, and the clause itself is pinned by the row below.",
          "value": "crash recovery is unsupported on \\S+ \\((?:no durable filesystem identity|remote filesystem|volatile filesystem)\\)\\. Merge will continue\\. Use --filesystem-strict to refuse\\.(?: Selected-root and --preserve abort may refuse until the workspace is on a handle-capable volume\\.)?"
        },
        {
          "id": "operation_model_handle_fail_reverse_door_limit",
          "value": "Selected-root and --preserve abort may refuse until the workspace is on a handle-capable volume."
        },
        {
          "id": "operation_model_handle_fail_escape",
          "value": "run `gwz merge --abort` there, adding\n`--preserve` if that was the door that refused"
        },
        {
          "id": "operation_model_one_record_for_every_start",
          "value": "Ordinary, `--ff-only`, custom-message and `--no-ff` starts all write the same merge record and reach this door alike"
        }
      ],
      "forbidden": [
        {
          "id": "must_not_claim_ordinary_starts_write_v0",
          "match": "regex",
          "comment": "Replaces the milestone-era guards for user-facing prose. The claim this package removed was that an ordinary, `--ff-only` or custom-message start writes a v0 record; both orderings of that sentence are caught. Scoped per source rather than globally, because the historical proposal/plan/design documents say exactly this about their own date and are correct to. Verified non-vacuous: it fires on the three sentences this package deleted (OperationModel `:246`, merge.md `:383`, MachineOutput `:336`) and on nothing in the current tree.",
          "value": "(?i)(?:(?:ordinary|custom-message|`?--ff-only`?)[^.\\n]{0,140}?\\bwrit\\w*[^.\\n]{0,40}?\\bv0\\b|\\bwrit\\w*[^.\\n]{0,20}\\bv0\\s+record[^.\\n]{0,80}(?:ordinary|custom-message|`?--ff-only`?))"
        },
        {
          "id": "must_not_claim_a_merge_record_is_migrated",
          "match": "regex",
          "comment": "There is no migration, upgrade, conversion or adapter for a merge record: an open pre-0.14 record is refused and an archived one projects read-only. Same per-source scoping and reason as the row above. Verified non-vacuous: it fires on the three sentences this package deleted (OperationModel `:248`, MachineOutput `:245`, MergeRecovery `:316`) and on nothing in the current tree.",
          "value": "(?i)(?:\\b(?:record|records|evidence)\\b[^.\\n]{0,140}?\\b(?:is|are|was|were|been|be|gets?)\\s+(?:automatically\\s+|not\\s+)*(?:migrat|upgrad|convert|adapt)\\w*|\\bupgrade\\s+to\\s+the\\s+v1\\b)"
        }
      ]
    },
    {
      "id": "merge_command",
      "path": "gwz-cli/docs/commands/merge.md",
      "required": [
        {
          "id": "merge_command_synopsis_lists_no_ff",
          "value": "gwz merge <source> [--dry-run] [--ff-only] [--no-ff] [--filesystem-strict] [-m <message>]"
        },
        {
          "id": "merge_command_synopsis_lists_filesystem_strict",
          "value": "[--ff-only] [--no-ff] [--filesystem-strict]"
        },
        {
          "id": "merge_command_no_ff_creates_a_merge_commit",
          "value": "`--no-ff` always creates a merge commit, even where a fast-forward is possible. It is the counterpart to `--ff-only`; supplying both together is rejected."
        },
        {
          "id": "merge_command_every_start_writes_one_record",
          "value": "Every start writes the same coordinated merge record — ordinary, `--ff-only`, custom-message and `--no-ff` alike — so status, continue, abort and recovery behave identically whichever way the merge was started."
        },
        {
          "id": "merge_command_pre_014_refusal_sentence",
          "comment": "The refusal is frozen verbatim in gwz-core (`workspace_ops/merge/open_record.rs::pre_014_merge_error`). The command page must print the same sentence a user actually sees.",
          "value": "this is a pre-0.14 merge; use gwz 0.13.0 (the last release before 0.14) to continue or abort"
        },
        {
          "id": "merge_command_handles_ok_documented",
          "value": "Its `handles_ok` field is `false` on such a volume, `true` on a below-bar volume whose handles work, and `null` above the bar"
        }
      ],
      "forbidden": [
        {
          "id": "merge_command_no_ff_must_not_be_deferred_again",
          "match": "regex",
          "value": "(?i)(?:`?--no-ff`?|no-ff)\\s+(?:is|remains)\\s+(?:not\\s+yet\\s+(?:available|supported|implemented|released)|hidden)"
        },
        {
          "id": "must_not_claim_ordinary_starts_write_v0",
          "match": "regex",
          "comment": "Replaces the milestone-era guards for user-facing prose. The claim this package removed was that an ordinary, `--ff-only` or custom-message start writes a v0 record; both orderings of that sentence are caught. Scoped per source rather than globally, because the historical proposal/plan/design documents say exactly this about their own date and are correct to. Verified non-vacuous: it fires on the three sentences this package deleted (OperationModel `:246`, merge.md `:383`, MachineOutput `:336`) and on nothing in the current tree.",
          "value": "(?i)(?:(?:ordinary|custom-message|`?--ff-only`?)[^.\\n]{0,140}?\\bwrit\\w*[^.\\n]{0,40}?\\bv0\\b|\\bwrit\\w*[^.\\n]{0,20}\\bv0\\s+record[^.\\n]{0,80}(?:ordinary|custom-message|`?--ff-only`?))"
        },
        {
          "id": "must_not_claim_a_merge_record_is_migrated",
          "match": "regex",
          "comment": "There is no migration, upgrade, conversion or adapter for a merge record: an open pre-0.14 record is refused and an archived one projects read-only. Same per-source scoping and reason as the row above. Verified non-vacuous: it fires on the three sentences this package deleted (OperationModel `:248`, MachineOutput `:245`, MergeRecovery `:316`) and on nothing in the current tree.",
          "value": "(?i)(?:\\b(?:record|records|evidence)\\b[^.\\n]{0,140}?\\b(?:is|are|was|were|been|be|gets?)\\s+(?:automatically\\s+|not\\s+)*(?:migrat|upgrad|convert|adapt)\\w*|\\bupgrade\\s+to\\s+the\\s+v1\\b)"
        }
      ]
    },
    {
      "id": "releases",
      "path": "gwz-cli/docs/Releases.md",
      "required": [
        {
          "id": "releases_unreleased_compatibility",
          "value": "## Unreleased Compatibility Notes"
        },
        {
          "id": "releases_merge_surface",
          "value": "Coordinated merge start, dry-run, status, continue, and safe abort are available together."
        },
        {
          "id": "releases_recovery_guidance",
          "value": "Recovery must not substitute raw `git merge --abort` for the coordinated operation."
        },
        {
          "id": "releases_custom_message_current",
          "value": "or the body supplied with `-m`"
        },
        {
          "id": "releases_close_open_merge_before_upgrading",
          "value": "**Close any open coordinated merge before you upgrade.**"
        },
        {
          "id": "releases_pre_014_refusal_sentence",
          "value": "this is a pre-0.14 merge; use gwz 0.13.0 (the last release before 0.14) to continue or abort"
        }
      ],
      "forbidden": [
        {
          "id": "must_not_claim_ordinary_starts_write_v0",
          "match": "regex",
          "comment": "Replaces the milestone-era guards for user-facing prose. The claim this package removed was that an ordinary, `--ff-only` or custom-message start writes a v0 record; both orderings of that sentence are caught. Scoped per source rather than globally, because the historical proposal/plan/design documents say exactly this about their own date and are correct to. Verified non-vacuous: it fires on the three sentences this package deleted (OperationModel `:246`, merge.md `:383`, MachineOutput `:336`) and on nothing in the current tree.",
          "value": "(?i)(?:(?:ordinary|custom-message|`?--ff-only`?)[^.\\n]{0,140}?\\bwrit\\w*[^.\\n]{0,40}?\\bv0\\b|\\bwrit\\w*[^.\\n]{0,20}\\bv0\\s+record[^.\\n]{0,80}(?:ordinary|custom-message|`?--ff-only`?))"
        },
        {
          "id": "must_not_claim_a_merge_record_is_migrated",
          "match": "regex",
          "comment": "There is no migration, upgrade, conversion or adapter for a merge record: an open pre-0.14 record is refused and an archived one projects read-only. Same per-source scoping and reason as the row above. Verified non-vacuous: it fires on the three sentences this package deleted (OperationModel `:248`, MachineOutput `:245`, MergeRecovery `:316`) and on nothing in the current tree.",
          "value": "(?i)(?:\\b(?:record|records|evidence)\\b[^.\\n]{0,140}?\\b(?:is|are|was|were|been|be|gets?)\\s+(?:automatically\\s+|not\\s+)*(?:migrat|upgrad|convert|adapt)\\w*|\\bupgrade\\s+to\\s+the\\s+v1\\b)"
        }
      ]
    },
    {
      "id": "machine_output",
      "path": "gwz-cli/docs/MachineOutput.md",
      "required": [
        {
          "id": "machine_output_merge_json",
          "value": "## Merge JSON"
        },
        {
          "id": "machine_output_current_shape",
          "value": "Merge responses use the normal response envelope and populate its `merge` field."
        },
        {
          "id": "machine_output_additive_keys",
          "value": "GWZ is pre-1.0, so strict consumers must tolerate additive keys"
        },
        {
          "id": "machine_output_open_pre_014_never_projects",
          "value": "a pre-0.14 record that is still **open** is refused rather than projected, and no response carries it"
        },
        {
          "id": "machine_output_crash_recovery_fields_are_explicit_nulls",
          "comment": "Measured against `gwz-cli/src/merge_render.rs::merge_crash_recovery_json`: all three optional fields are emitted as keys and carry `null`. Only the `crash_recovery` object itself is omitted. A consumer told otherwise would branch on key presence and be wrong.",
          "value": "`filesystem`, `gap` and `handles_ok` are always **present as keys** and carry an explicit `null` when the decision has no value for them."
        }
      ],
      "forbidden": [
        {
          "id": "must_not_claim_ordinary_starts_write_v0",
          "match": "regex",
          "comment": "Replaces the milestone-era guards for user-facing prose. The claim this package removed was that an ordinary, `--ff-only` or custom-message start writes a v0 record; both orderings of that sentence are caught. Scoped per source rather than globally, because the historical proposal/plan/design documents say exactly this about their own date and are correct to. Verified non-vacuous: it fires on the three sentences this package deleted (OperationModel `:246`, merge.md `:383`, MachineOutput `:336`) and on nothing in the current tree.",
          "value": "(?i)(?:(?:ordinary|custom-message|`?--ff-only`?)[^.\\n]{0,140}?\\bwrit\\w*[^.\\n]{0,40}?\\bv0\\b|\\bwrit\\w*[^.\\n]{0,20}\\bv0\\s+record[^.\\n]{0,80}(?:ordinary|custom-message|`?--ff-only`?))"
        },
        {
          "id": "must_not_claim_a_merge_record_is_migrated",
          "match": "regex",
          "comment": "There is no migration, upgrade, conversion or adapter for a merge record: an open pre-0.14 record is refused and an archived one projects read-only. Same per-source scoping and reason as the row above. Verified non-vacuous: it fires on the three sentences this package deleted (OperationModel `:248`, MachineOutput `:245`, MergeRecovery `:316`) and on nothing in the current tree.",
          "value": "(?i)(?:\\b(?:record|records|evidence)\\b[^.\\n]{0,140}?\\b(?:is|are|was|were|been|be|gets?)\\s+(?:automatically\\s+|not\\s+)*(?:migrat|upgrad|convert|adapt)\\w*|\\bupgrade\\s+to\\s+the\\s+v1\\b)"
        }
      ]
    },
    {
      "id": "generated_cli",
      "path": "gwz-cli/docs/CLI.md",
      "required": [
        {
          "id": "generated_cli_merge_section",
          "value": "### `gwz merge`"
        },
        {
          "id": "generated_cli_current_usage",
          "value": "Usage: gwz merge [source] [--dry-run] [--ff-only] [--no-ff] [--filesystem-strict] [-m <message>]",
          "comment": "Generated from the clap `override_usage` in `gwz-cli/src/globalargs/parser.rs`; regenerate with `python scripts/generate_cli_reference.py --write` rather than editing `docs/CLI.md`. Until 2026-09-05 this row pinned a usage line that omitted `--no-ff` and `--filesystem-strict` while the Options block below it listed both."
        },
        {
          "id": "generated_cli_ff_only",
          "value": "--ff-only Require every selected repository to merge by fast-forward"
        },
        {
          "id": "generated_cli_custom_message",
          "value": "--message <message> Use a custom merge commit-message body"
        },
        {
          "id": "generated_cli_no_ff",
          "value": "--no-ff Always create a merge commit, even when a fast-forward is possible"
        }
      ],
      "forbidden": []
    },
    {
      "id": "merge_recovery_guide",
      "path": "gwz-cli/docs/MergeRecovery.md",
      "required": [
        {
          "id": "merge_recovery_guide_recovery_path_is_start_independent",
          "value": "Ordinary, `--ff-only`, custom-message and `--no-ff` starts all use the same coordinated rollback, so this case never depends on how the merge was started."
        },
        {
          "id": "merge_recovery_guide_pre_014_is_not_a_wedge",
          "comment": "A pre-0.14 record is refused, not wedged. The runbook must send that reader to a 0.13.x build rather than to the parking procedure, which would strand a record that closes normally.",
          "value": "Install a 0.13.x build, close the merge with it — `gwz merge --status`, then `gwz merge --continue` or `gwz merge --abort` — and return to 0.14."
        }
      ],
      "forbidden": [
        {
          "id": "must_not_claim_ordinary_starts_write_v0",
          "match": "regex",
          "comment": "Replaces the milestone-era guards for user-facing prose. The claim this package removed was that an ordinary, `--ff-only` or custom-message start writes a v0 record; both orderings of that sentence are caught. Scoped per source rather than globally, because the historical proposal/plan/design documents say exactly this about their own date and are correct to. Verified non-vacuous: it fires on the three sentences this package deleted (OperationModel `:246`, merge.md `:383`, MachineOutput `:336`) and on nothing in the current tree.",
          "value": "(?i)(?:(?:ordinary|custom-message|`?--ff-only`?)[^.\\n]{0,140}?\\bwrit\\w*[^.\\n]{0,40}?\\bv0\\b|\\bwrit\\w*[^.\\n]{0,20}\\bv0\\s+record[^.\\n]{0,80}(?:ordinary|custom-message|`?--ff-only`?))"
        },
        {
          "id": "must_not_claim_a_merge_record_is_migrated",
          "match": "regex",
          "comment": "There is no migration, upgrade, conversion or adapter for a merge record: an open pre-0.14 record is refused and an archived one projects read-only. Same per-source scoping and reason as the row above. Verified non-vacuous: it fires on the three sentences this package deleted (OperationModel `:248`, MachineOutput `:245`, MergeRecovery `:316`) and on nothing in the current tree.",
          "value": "(?i)(?:\\b(?:record|records|evidence)\\b[^.\\n]{0,140}?\\b(?:is|are|was|were|been|be|gets?)\\s+(?:automatically\\s+|not\\s+)*(?:migrat|upgrad|convert|adapt)\\w*|\\bupgrade\\s+to\\s+the\\s+v1\\b)"
        }
      ]
    }
  ],
  "retired": [
    {
      "id": "releases_must_not_claim_no_ff_yet",
      "source": "releases",
      "was": "forbidden: \"`--no-ff` is available\"",
      "retired": "2026-09-05, M5d documentation package",
      "why": "Stale by the one test that matters: the row forbade a sentence that is now TRUE. `--no-ff` has been available since v0.11.0, so the milestone-era guard against announcing it early could only fire on correct release prose. Retired rather than loosened -- there is no narrower form of it that has a true subject."
    },
    {
      "id": "machine_output_must_not_claim_no_ff_yet",
      "source": "machine_output",
      "was": "forbidden: \"`--no-ff` is available\"",
      "retired": "2026-09-05, M5d documentation package",
      "why": "The identical stale guard on a second source, retired for the identical reason. Listed separately so neither disappears silently."
    },
    {
      "id": "merge_command_no_ff_writes_v1_while_ordinary_stays_v0",
      "source": "merge_command",
      "was": "required: \"A `--no-ff` start writes a v1 coordinated merge record and publishes a two-parent integration commit. Ordinary and custom-message starts continue to write v0 records.\"",
      "retired": "2026-09-05, M5d documentation package",
      "why": "The row id and its assertion both became false: v1 is the only merge, so ordinary, `--ff-only`, custom-message and `--no-ff` starts all write the same record. Replaced by `merge_command_every_start_writes_one_record`, which pins the statement that is true instead."
    },
    {
      "id": "merge_recovery_guide_no_ff_recovery_path_bound",
      "source": "merge_recovery_guide",
      "was": "required: \"**This is the `--no-ff` recovery path.** `--no-ff` starts always use it; an ordinary start can also reach it once its record is migrated during abort. Match on the message, not on how you started the merge.\"",
      "retired": "2026-09-05, M5d documentation package",
      "why": "There is no migration: no record is converted during abort, because there is only one record format. Replaced by `merge_recovery_guide_recovery_path_is_start_independent`, which keeps the row's real purpose -- match on the message, not on how the merge was started -- with a true reason under it."
    }
  ]
}
